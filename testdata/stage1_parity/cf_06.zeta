module probe.cf06;
fn nested(n: Int) -> Int {
  let mut i: Int = 0;
  let mut total: Int = 0;
  while i < n {
    let mut j: Int = 0;
    while j < i {
      total = total + j;
      j = j + 1;
    }
    i = i + 1;
  }
  return total;
}
