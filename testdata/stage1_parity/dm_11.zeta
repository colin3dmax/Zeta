module probe.dm11;
fn f(a: Int, b: Int) -> Int {
  match (a + b) * 2 {
    0 -> {
      log(a);
      match b {
        1 -> { sink(b); return 1; }
        _ -> { return 2; }
      }
      log(b);
      return 3;
    }
    _ -> { return 4; }
  }
  return 0;
}
