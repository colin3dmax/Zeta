module probe.cf11;
fn breakloops(n: Int) -> Int {
  let mut i: Int = 0;
  while true {
    if i >= n {
      break;
    }
    while i < n {
      if i == 3 {
        break;
      }
      i = i + 1;
      continue;
    }
    i = i + 1;
  }
  return i;
}
